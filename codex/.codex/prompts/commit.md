---
description: Bundle pending changes into a commit via the git-commit-specialist subagent.
---

If the current pending changes can reasonably be compacted into a single commit, delegate to the `git-commit-specialist` subagent to stage, commit, and update CHANGELOG entries. Otherwise do nothing.
